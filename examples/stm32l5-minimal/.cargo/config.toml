[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = "probe-rs run --always-print-stacktrace --chip STM32L552RC"
rustflags = [
  "-C", "link-arg=-Tlink.x",
]

[build]
target = "thumbv8m.main-none-eabihf"
